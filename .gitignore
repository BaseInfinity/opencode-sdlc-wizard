node_modules/
.cache/
.DS_Store
*.log
*.tmp
.env
.env.*
!.env.example

# Build artifacts (Phase A may add)
dist/
build/

# Test artifacts
coverage/
.nyc_output/
*.bak

# IDE
.vscode/
.idea/
*.swp
*~

# Local-only state (matches parent wizard pattern)
.metrics/
.reviews/latest-review.md
.reviews/response.json

# Dogfood artifacts — agentic-sdlc-wizard parent installs locally for SDLC
# enforcement while developing this OpenCode sibling. These are CC-specific
# dev tooling, not part of what this wizard ships, so don't track them.
.claude/
CLAUDE_CODE_SDLC_WIZARD.md
